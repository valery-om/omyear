{
  "name": "omyear-build-week",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "test": "node --test pipeline/tests/pipeline.test.mjs",
    "build": "npm --prefix web run build",
    "audit": "node scripts/audit-public.mjs",
    "audit:deps": "npm --prefix web audit --audit-level=moderate",
    "check": "npm run test && npm run build && npm run audit && npm run audit:deps"
  }
}
